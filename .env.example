# Example environment variables for the Edge Function / server
# Copy this to a real .env (or configure in your hosting platform) and fill with real secrets.

# Supabase
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# Paystack (required for billing features)
# Use PAYSTACK_SECRET_KEY for server-side requests (transaction initialize/verify/webhook)
# Use PAYSTACK_PUBLIC_KEY in frontend where safe to expose (for client-side Paystack usage)
PAYSTACK_SECRET_KEY=
PAYSTACK_PUBLIC_KEY=

# Optional alternative env var names supported by the server
# PAYSTACK_SECRET and PAYSTACK_PUBLIC
